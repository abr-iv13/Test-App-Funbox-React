import React from 'react'

import './FooterSelected.css'

const FooterSelected = ({ footer }) => {
    return (
        <p className="footer-selected">{footer}</p>
    );
};
export default FooterSelected;